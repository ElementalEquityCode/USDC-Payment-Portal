import React from 'react';
import styles from './ValueEnteredLabel.module.css';

const ValueEnteredLabel = () => (
  <h3 className={styles.valueEnteredLabel}>$0.00</h3>
);

export default ValueEnteredLabel;
